type Company {
  id: ID!
  name: String!
  slug: String!
  created_at: DateTime!
  updated_at: DateTime!
  
  # Relations (can be added later with proper directives)
  # users: [User!]! @hasMany
  # employees: [Employee!]! @hasMany
  # shifts: [Shift!]! @hasMany
}

input CompanyInput {
  name: String!
  slug: String!
}

input CompanyUpdateInput {
  id: ID!
  name: String
  slug: String
}